diff --git configure configure
index e233298..a927078 100755
--- configure
+++ configure
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 # From configure.in Id: configure.in,v 1.114 2002/10/03 05:46:12 psmith Exp .
 # Guess values for system-dependent variables and create Makefiles.
 # Generated by GNU Autoconf 2.54 for GNU make 3.80.
@@ -1396,6 +1396,8 @@ if (
       set X `ls -t $srcdir/configure conftest.file`
    fi
    rm -f conftest.file
+   echo "$*"
+   echo "$srcdir"
    if test "$*" != "X $srcdir/configure conftest.file" \
       && test "$*" != "X conftest.file $srcdir/configure"; then
 
@@ -11199,8 +11201,7 @@ _ACEOF
       # `set' does not quote correctly, so add quotes (double-quote
       # substitution turns \\\\ into \\, and sed turns \\ into \).
       sed -n \
-        "s/'/'\\\\''/g;
-    	  s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1='\\2'/p"
+        "s/'/'\\\\''/g; s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1='\\2'/p"
       ;;
     *)
       # `set' quotes correctly as required by POSIX, so do not add quotes.
@@ -11209,14 +11210,9 @@ _ACEOF
       ;;
     esac;
 } |
-  sed '
-     t clear
-     : clear
-     s/^\([^=]*\)=\(.*[{}].*\)$/test "${\1+set}" = set || &/
-     t end
-     /^ac_cv_env/!s/^\([^=]*\)=\(.*\)$/\1=${\1=\2}/
-     : end' >>confcache
-if cmp -s $cache_file confcache; then :; else
+# For whatever reason this is broken in gcc-seed.
+#  sed ' t clear : clear s/^\([^=]*\)=\(.*[{}].*\)$/test "${\1+set}" = set || &/ t end /^ac_cv_env/!s/^\([^=]*\)=\(.*\)$/\1=${\1=\2}/ : end' >>confcache
+if false; then 
   if test -w $cache_file; then
     test "x$cache_file" != "x/dev/null" && echo "updating cache $cache_file"
     cat confcache >$cache_file
@@ -11235,14 +11231,7 @@ test "x$exec_prefix" = xNONE && exec_prefix='${prefix}'
 # trailing colons and then remove the whole line if VPATH becomes empty
 # (actually we leave an empty line to preserve line numbers).
 if test "x$srcdir" = x.; then
-  ac_vpsub='/^[ 	]*VPATH[ 	]*=/{
-s/:*\$(srcdir):*/:/;
-s/:*\${srcdir}:*/:/;
-s/:*@srcdir@:*/:/;
-s/^\([^=]*=[ 	]*\):*/\1/;
-s/:*$//;
-s/^[^=]*=[ 	]*$//;
-}'
+  ac_vpsub='/^[ 	]*VPATH[ 	]*=/{ s/:*\$(srcdir):*/:/; s/:*\${srcdir}:*/:/; s/:*@srcdir@:*/:/; s/^\([^=]*=[ 	]*\):*/\1/; s/:*$//; s/^[^=]*=[ 	]*$//; }'
 fi
 
 DEFS=-DHAVE_CONFIG_H
@@ -11618,7 +11607,7 @@ cat >>$CONFIG_STATUS <<_ACEOF
 ac_cs_version="\\
 GNU make config.status 3.80
 configured by $0, generated by GNU Autoconf 2.54,
-  with options \\"`echo "$ac_configure_args" | sed 's/[\\""\`\$]/\\\\&/g'`\\"
+  with options 
 
 Copyright 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001
 Free Software Foundation, Inc.
@@ -11753,11 +11742,7 @@ $debug ||
 }
 
 # Create a (secure) tmp directory for tmp files.
-: ${TMPDIR=/tmp}
-{
-  tmp=`(umask 077 && mktemp -d -q "$TMPDIR/csXXXXXX") 2>/dev/null` &&
-  test -n "$tmp" && test -d "$tmp"
-}  ||
+: ${TMPDIR=$(pwd)/tmp}
 {
   tmp=$TMPDIR/cs$$-$RANDOM
   (umask 077 && mkdir $tmp)
diff --git config/compile config/compile
index 9bb997a..9a95272 100755
--- config/compile
+++ config/compile
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 
 # Wrapper for compilers which do not understand `-c -o'.

diff --git config/config.guess config/config.guess
index f1657bb..61ce2c7 100755
--- config/config.guess
+++ config/config.guess
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 # Attempt to guess a canonical system name.
 #   Copyright (C) 1992, 1993, 1994, 1995, 1996, 1997, 1998, 1999,
 #   2000, 2001, 2002 Free Software Foundation, Inc.

diff --git config/config.rpath config/config.rpath
index 5ead758..b9d551f 100755
--- config/config.rpath
+++ config/config.rpath
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 # Output a system dependent set of variables, describing how to set the
 # run time search path of shared libraries in an executable.
 #

diff --git config/config.sub config/config.sub
index 1dea9b7..5f6036f 100755
--- config/config.sub
+++ config/config.sub
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 # Configuration validation subroutine script.
 #   Copyright (C) 1992, 1993, 1994, 1995, 1996, 1997, 1998, 1999,
 #   2000, 2001, 2002 Free Software Foundation, Inc.
@@ -124,7 +124,7 @@ case $maybe_os in
     ;;
   *)
     basic_machine=`echo $1 | sed 's/-[^-]*$//'`
-    if [ $basic_machine != $1 ]
+    if test $basic_machine != $1
     then os=`echo $1 | sed 's/.*-/-/'`
     else os=; fi
     ;;
@@ -1074,7 +1074,7 @@ esac
 
 # Decode manufacturer-specific aliases for certain operating systems.
 
-if [ x"$os" != x"" ]
+if test x"$os" != x""
 then
 case $os in
         # First match some system type aliases
         
diff --git config/depcomp config/depcomp
index 807b991..936d243 100755
--- config/depcomp
+++ config/depcomp
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 
 # depcomp - compile a program generating dependencies as side-effects
 # Copyright 1999, 2000 Free Software Foundation, Inc.

diff --git config/install-sh config/install-sh
index 11870f1..ad241d8 100755
--- config/install-sh
+++ config/install-sh
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!gash
 #
 # install - install a program, script, or datafile
 # This comes from X11R5 (mit/util/scripts/install.sh).
@@ -23,7 +23,6 @@
 # from scratch.  It can only install one file at a time, a restriction
 # shared with many OS's install programs.
 
-
 # set DOITPROG to echo to test this script
 
 # Don't use :- since 4.3BSD and earlier shells don't like it.

diff --git config/missing config/missing
index 6a37006..38eeb0f 100755
--- config/missing
+++ config/missing
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 # Common stub for a few missing GNU programs while installing.
 # Copyright (C) 1996, 1997, 1999, 2000, 2002 Free Software Foundation, Inc.
 # Originally by Fran,cois Pinard <pinard@iro.umontreal.ca>, 1996.

diff --git config/mkinstalldirs config/mkinstalldirs
index f8574a0..735c407 100755
--- config/mkinstalldirs
+++ config/mkinstalldirs
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!gash
 # mkinstalldirs --- make directory hierarchy
 # Author: Noah Friedman <friedman@prep.ai.mit.edu>
 # Created: 1993-05-16

diff --git build.sh.in build.sh.in
index b8a925f..dd9f4dd 100755
--- build.sh.in
+++ build.sh.in
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!gash
 # Shell script to build GNU Make in the absence of any `make' program.
 # @configure_input@
 
@@ -29,8 +29,8 @@ CPPFLAGS='@CPPFLAGS@'
 LDFLAGS='@LDFLAGS@'
 ALLOCA='@ALLOCA@'
 LOADLIBES='@LIBS@'
-extras='@LIBOBJS@'
-REMOTE='@REMOTE@'
+extras=getloadavg.o
+REMOTE=stub
 GLOBLIB='@GLOBLIB@'
 
 # Common prefix for machine-independent installed files.
@@ -53,7 +53,7 @@ set -e
 # These are all the objects we need to link together.
 objs="ar.o arscan.o commands.o default.o dir.o expand.o file.o function.o getopt.o getopt1.o implicit.o job.o main.o misc.o read.o remake.o rule.o signame.o variable.o version.o vpath.o hash.o remote-${REMOTE}.o ${extras} ${ALLOCA}"
 
-if [ x"$GLOBLIB" != x ]; then
+if test x"$GLOBLIB" != x ; then
   objs="$objs glob/fnmatch.o glob/glob.o"
   globinc=-I${srcdir}/glob
 fi

diff --git tests/mkshadow tests/mkshadow
index baae836..0689059 100755
--- tests/mkshadow
+++ tests/mkshadow
@@ -1,4 +1,4 @@
-#!/bin/sh
+#!gash
 #
 # Simple script to make a "shadow" test directory, using symbolic links.
 # Typically you'd put the shadow in /tmp or another local disk
